{
  "name": "@prw/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome MV3 extension that renders the PR walkthrough tour and the select-code-and-ask modal.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prw/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2",
    "typescript": "^5.9.3"
  }
}
