{
  "name": "@kvasir/runes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure shared contract between the server and the extension: zod-validated walkthrough spec types, the fixed bridge port, PR-URL parsing, and diff anchors.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
